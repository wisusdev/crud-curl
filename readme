Para levantar nueva API debemos correr el siguiente comando para instalar el paquete necesario.
https://github.com/typicode/json-server

npm install -g json-server

Para correr el servicio usamos:
json-server --watch db.json

Para eecutar PHP se debe ejcutar el siguiente comando
php -S localhost:8000
